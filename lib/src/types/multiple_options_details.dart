/// A record containing the [value], the [title] and an optional [subtitle] of an option of a [SettingMultipleOptionsTile].
typedef MultipleOptionsDetails = ({
  Object value,
  String title,
  String? subtitle,
});
